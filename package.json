{
  "name": "@musicociel/song-formats",
  "description": "Library allowing to import/export songs between the Musicociel file format and other formats including Opensong and ChordPro.",
  "version": "0.0.1",
  "author": "DivDE <divde@musicociel.fr>",
  "license": "AGPL-3.0",
  "scripts": {
    "watch": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "prepublish": "yarn build",
    "test": "true"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/musicociel/song-formats.git"
  },
  "devDependencies": {
    "typescript": "^2.3.2"
  },
  "dependencies": {
    "diacritics": "^1.3.0",
    "json-stable-stringify": "^1.0.1",
    "sha.js": "^2.4.8",
    "xmldom": "^0.1.27"
  },
  "files": [
    "src",
    "build"
  ]
}
